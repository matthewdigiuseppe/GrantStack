{"_comment": "One JSON object per line, appended by /grantstack:learn. Proposal-specific conventions Claude should remember: the acronym to use in headers, the framing decision behind the ambition, the panel you are writing for. Facts that belong in .grantstack/config.yaml (scheme, acronym, title, budget, status) go there instead."}
